public let AEROSPACE_WINDOW_ID = "AEROSPACE_WINDOW_ID"
public let AEROSPACE_WORKSPACE = "AEROSPACE_WORKSPACE"
public let AEROSPACE_FOCUSED_WORKSPACE = "AEROSPACE_FOCUSED_WORKSPACE"
public let AEROSPACE_PREV_WORKSPACE = "AEROSPACE_PREV_WORKSPACE"
/// "true" when the newly focused workspace is in floating-workspaces, so exec-on-workspace-change
/// callbacks don't have to duplicate that list or ask the CLI (which is at its slowest mid-switch).
public let AEROSPACE_FOCUSED_WORKSPACE_IS_FLOATING = "AEROSPACE_FOCUSED_WORKSPACE_IS_FLOATING"
